import { InputProtectorDirective } from './input-protector.directive';

describe('InputProtectorDirective', () => {
  it('should create an instance', () => {
    const directive = new InputProtectorDirective();
    expect(directive).toBeTruthy();
  });
});
